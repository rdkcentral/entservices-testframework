set(autostart ${PLUGIN_MOCKAUTHSERVICEPLUGIN_AUTOSTART})

if(PLUGIN_MOCKAUTHSERVICEPLUGIN_STARTUPORDER)
set (startuporder ${PLUGIN_MOCKAUTHSERVICEPLUGIN_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_DUMMYPLUGIN_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
end()
ans(configuration)
